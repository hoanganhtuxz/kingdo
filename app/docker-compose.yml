 version: '3.3'
 services:
   service:
     tty: true
     image: node:18.12.0
     volumes:
       - '.:/code'
     working_dir: /code
     command: >
       sh -c  "yarn install && yarn build && yarn start"
     ports:
       - 8006:3000
     restart: always
